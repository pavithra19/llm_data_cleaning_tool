# llm_data_cleaning_tool
A small tool I built to speed up CSV data cleaning. An auditable checks with pandas (missing values, duplicates), a compact full-dataset summary + random sample (up to 50 rows).
